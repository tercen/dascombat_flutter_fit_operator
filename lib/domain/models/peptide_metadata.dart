/// Row metadata for one peptide/protein.
class PeptideMetadata {
  /// Peptide/protein identifier (e.g., "ANXA1_209_221").
  final String id;

  const PeptideMetadata({
    required this.id,
  });
}
